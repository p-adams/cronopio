export { createFileDB } from "./lib/FileDB.ts";
/**
 * Next steps:
 *  deno bundle mod.ts bundle.js
 *  deno publish --allow-read --allow-write --allow-net
 *  import { createFileDB } from 'https://deno.land/x/my_package/mod.js';
 */
